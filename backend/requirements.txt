# Exact pins, not wildcards: an unreviewed patch/minor bump landing silently on
# the next `pip install` is exactly how a supply-chain compromise or a breaking
# behavior change slips into a production build. Bump deliberately and re-run
# `pip-audit -r requirements.txt` when you do.

# ── Core ──────────────────────────────────────────────────────────────────────
Django==5.2.15
djangorestframework==3.16.0
django-environ==0.11.2

# ── Database ──────────────────────────────────────────────────────────────────
psycopg[binary]==3.3.4

# ── Storage ───────────────────────────────────────────────────────────────────
django-storages[s3]==1.14.6
boto3==1.34.162

# ── API schema ────────────────────────────────────────────────────────────────
drf-spectacular==0.27.2

# ── Audit / history ───────────────────────────────────────────────────────────
django-simple-history==3.7.0

# ── Auth / security ───────────────────────────────────────────────────────────
django-axes==7.0.1
django-csp==3.8

# ── PDF / DSC signing ─────────────────────────────────────────────────────────
pyHanko[image-support,opentype]==0.22.0
pyhanko-certvalidator==0.26.8
reportlab==4.5.1

# ── Working-day calendar ──────────────────────────────────────────────────────
holidays==0.46

# ── File type detection (magic bytes, AC-19) ─────────────────────────────────
python-magic==0.4.27

# ── Email ─────────────────────────────────────────────────────────────────────
# Resend SMTP — no extra package needed; Django's SMTP backend handles it
